#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main(int argc, char *argv[]) {
  int n;
  cin >> n;
  for (size_t i = 0; i < n; i++) {
    int k, q;
    cin >> k >> q;
    int a[k];
    for (size_t j = 0; j < k; j++) {
      cin >> a[j];
    }
    for (size_t j = 0; j < q; j++) {
      int c;
      cin >> c;
      
      cout << count << " ";
    }
  }
}
